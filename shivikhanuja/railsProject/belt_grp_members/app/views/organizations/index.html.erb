<p>Hi,  <%= current_user.first_name.capitalize %>!</p>
Here are the organizations you may join:

    <% @organization.each do |organization| %>
        <p><a href="<%= organizations_show_path organization.id%>"><%= organization.name %></a></p>
        <p><%= organization.membering.count %></p>
        <p><%= organization.description %></p>
    <% end %>

<h5>Create a new Organization:</h5>

<div class ="row">
    <div class="col-md-2"></div>
    <div class="col-md-8">
        <form action="<%= create_organizations_path %>" method="post" class="form">
            <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token  %>"/>
            <div class="form-group">
                <input type="text" name= "organization[name]" class="form-control" id="name" placeholder="Name">                
            </div>
            <div class="form-group">                
                <input type="text" name= "organization[description]" class="form-control" id="description" placeholder="Description">
            </div>
            <div class="form-group">
                <button class="btn btn-primary" type="submit">Create</button>
            </div>
        </form>
    </div>
    <div class="col-md-2"></div>
</div>
